<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="clr-namespace:MNIST.Controls"
    xmlns:converter="clr-namespace:MNIST.Converters">
    <converter:ContentToTextConverter x:Key="ContentToTextConverter"/>
    <!-- Accented Brushes -->
    <SolidColorBrush x:Key="AccentBrushLight" Color="Gold"/>
    <SolidColorBrush x:Key="AccentBrush" Color="Goldenrod"/>
    <SolidColorBrush x:Key="AccentBrushDark" Color="DarkGoldenrod"/>
    <!-- Background Brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="#2B2B2B"/>
    <SolidColorBrush x:Key="LightBackgroundBrush" Color="#303030"/>
    <SolidColorBrush x:Key="DarkBackgroundBrush" Color="#202020"/>
    <SolidColorBrush x:Key="PitchBlackBackgroundBrush" Color="#000000"/>
    <!-- Foreground Brushes -->
    <SolidColorBrush x:Key="LightForegroundBrush" Color="#A2FFFFFF"/>
    <SolidColorBrush x:Key="DarkForegroundBrush" Color="#202020"/>
    <SolidColorBrush x:Key="GrayForegroundBrush" Color="#A2A2A2"/>
    <SolidColorBrush x:Key="DarkGrayForegroundBrush" Color="#727272"/>
    <SolidColorBrush x:Key="PreviewFontForegroundBrush" Color="#FFFFFF"/>
    
    <!-- Default Help Item Style-->
    <Style TargetType="control:HelpItemControl">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="control:HelpItemControl">
                    <Grid Margin="10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Border
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            CornerRadius="2"
                            Padding="8,3"
                            BorderThickness="1"
                            Background="{StaticResource AccentBrush}"
                            BorderBrush="{StaticResource AccentBrushDark}">
                            <TextBlock
                                FontSize="14"
                                FontWeight="Bold"
                                Opacity="1"
                                Text="{TemplateBinding HelpKey}"/>
                        </Border>
                        <TextBlock 
                            HorizontalAlignment="Center"
                            TextAlignment="Center"
                            Margin="0,5,0,0"
                            FontSize="13"
                            Foreground="{StaticResource DarkGrayForegroundBrush}"
                            Grid.Row="1" 
                            Text="{TemplateBinding HelpText}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Long Help Item Style -->
    <Style x:Key="HelpItemLongStyle" TargetType="control:HelpItemControl">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="control:HelpItemControl">
                    <Grid Opacity="1" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Border
                            Opacity="1"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Right"
                            CornerRadius="2"
                            Padding="10,4"
                            BorderThickness="1"
                            BorderBrush="{StaticResource AccentBrushDark}"
                            Background="{StaticResource AccentBrushLight}">
                            <TextBlock
                                FontSize="20"
                                FontWeight="Bold"
                                Opacity="1"
                                Text="{TemplateBinding HelpKey}"/>
                        </Border>
                        <TextBlock 
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            TextAlignment="Left"
                            FontSize="18"
                            Foreground="{StaticResource LightForegroundBrush}"
                            Grid.Column="2" 
                            Text="{TemplateBinding HelpText}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Accented Button Style -->
    <Style x:Key="AccentedButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrushDark}" />
        <Setter Property="Foreground" Value="{StaticResource PitchBlackBackgroundBrush}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="ButtonBody"
                        Padding="{TemplateBinding Padding}"
                        CornerRadius="2"
                        Background="{TemplateBinding Background}">
                        <ContentPresenter
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="ButtonBody" Value="0.8"/>
                        </Trigger>
                        <!-- Pressed -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsPressed"  Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="Opacity" TargetName="ButtonBody" Value="0.8"/>
                                <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <!-- Mouse Over -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsPressed" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="Background" Value="{StaticResource AccentBrushLight}" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Hyperlink Button Style-->
    <Style x:Key="HyperlinkButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource PreviewFontForegroundBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Opacity" Value="0.8" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <TextBlock 
                        x:Name="TextContent"
                        Focusable="False"
                        MinWidth="60"
                        Text="{TemplateBinding Content, Converter={StaticResource ContentToTextConverter}}"
                        Background="{TemplateBinding Background}"
                        Foreground="{TemplateBinding Foreground}"
                        FontSize="{TemplateBinding FontSize}"
                        FontWeight="{TemplateBinding FontWeight}"
                        FontStyle="{TemplateBinding FontStyle}"
                        FontFamily="{TemplateBinding FontFamily}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TextContent" Property="TextDecorations" Value="Underline"/>
                            <Setter TargetName="TextContent"  Property="Foreground" Value="{StaticResource AccentBrushLight}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter> 
    </Style>
    
    <!-- Accected List Box Item Style -->
    <Style x:Key="AccectedListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}" />
        <Setter Property="Opacity" Value="1" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Width" Value="160" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="BorderThickness" Value="0.5" />
        <Setter Property="BorderBrush" Value="{StaticResource GrayForegroundBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        Focusable="{TemplateBinding Focusable}"
                        x:Name="ListViewItemBox"
                        CornerRadius="3"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}"
                        Margin="{TemplateBinding Margin}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Focusable="{TemplateBinding Focusable}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource AccentBrushLight}" />
                            <Setter Property="Foreground" Value="{StaticResource DarkForegroundBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource AccentBrushDark}" />
                            <Setter Property="Foreground" Value="{StaticResource DarkForegroundBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource DarkForegroundBrush}" />
                        </MultiTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentedMenuItemStyle" TargetType="MenuItem">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border 
                        x:Name="Root"
                        BorderBrush="Transparent"
                        BorderThickness="1.5"
                        Margin="-1.5"
                        CornerRadius="3"
                        MinWidth="120"
                        Background="{TemplateBinding Background}"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter 
                                Grid.Column="0" 
                                Margin="10,2,8,2" 
                                ContentSource="Icon" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"/>
                            <ContentPresenter Margin="2" Grid.Column="1" ContentSource="Header" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrushDark}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>